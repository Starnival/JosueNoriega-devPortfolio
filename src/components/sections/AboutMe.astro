---
import GridItem from '../base/GridItem.astro';
import { basics } from '../../data/resume.json';
const { class: className, ...rest } = Astro.props;
const { summary } = basics;
---

<GridItem class:list={['section-about-me', className]} {...rest}>
    <h2>Sobre mí</h2>
    <p>{summary}</p>
</GridItem>

<style>
    .section-about-me {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        padding: 0 30px;
        gap: 8px;

        h2 {
            margin-bottom: 25px;
        }
    }
</style>

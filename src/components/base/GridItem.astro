---
const { class: className, ...rest } = Astro.props;
---

<section class:list={['grid-item', className]} {...rest}>
    <slot />
</section>

<style>
    .grid-item {
        min-width: 0;
        box-sizing: border-box; /* Evita que el padding afecte el tamaño del grid item */
        overflow: hidden; /* Evita que el contenido desborde y afecte el tamaño del item */
        padding: 1.5rem; /* 24px */

        border-radius: 1rem; /* 24px */
        background-color: var(--fondo-secundario);
        transition: all 0.3s ease-in-out;

        /* Estilo Neumórfico en reposo */
        box-shadow: 5px 5px 10px var(--sombra-oscura),
          inset  -5px -5px 10px var(--sombra-clara);
    }

    .grid-item:hover,
    .grid-item:focus-within {
        /* Estilo Neumórfico activo (al pasar el cursor o enfocar) */
        box-shadow: 8px 8px 16px var(--sombra-oscura),
           inset -8px -8px 16px var(--sombra-clara);
    }
</style>
